name: Local Engine Build Engine

on:
  push:
    paths:
      - "build-engine/**"
      - ".github/workflows/local-engine-build-engine.yml"
  pull_request:
    paths:
      - "build-engine/**"
      - ".github/workflows/local-engine-build-engine.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Validate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: build-engine

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: build-engine/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Validate Dockerfile builds
        run: docker build -t local-engine-build-engine:test .
