name: Local Engine Build Engine

on:
  push:
    paths:
      - "local-engine/build-engine/**"
      - ".github/workflows/local-engine-build-engine.yml"
  pull_request:
    paths:
      - "local-engine/build-engine/**"
      - ".github/workflows/local-engine-build-engine.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Validate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: local-engine/build-engine

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: local-engine/build-engine/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Validate Dockerfile builds
        run: docker build -t local-engine-build-engine:test .
