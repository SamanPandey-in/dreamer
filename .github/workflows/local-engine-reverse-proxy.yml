name: Local Engine Reverse Proxy

on:
  push:
    paths:
      - "reverse-proxy/**"
      - ".github/workflows/local-engine-reverse-proxy.yml"
  pull_request:
    paths:
      - "reverse-proxy/**"
      - ".github/workflows/local-engine-reverse-proxy.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Validate & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: reverse-proxy

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: reverse-proxy/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Syntax check
        run: node --check index.js

      - name: Run tests
        run: npm test
