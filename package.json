{
  "name": "dreamer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n api,worker,front,proxy -c blue,pink,green,yellow \"npm -C apps/api-server run dev\" \"npm -C apps/api-server run worker\" \"npm -C apps/frontend run dev\" \"npm -C apps/reverse-proxy run dev\"",
    "setup": "npm -C apps/api-server i && npm -C apps/frontend i && npm -C apps/reverse-proxy i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Saman Pandey",
  "github-url": "SamanPandey-in/dreamer",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
